import MealPlanScreen from '@/features/meal-planning/components/MealPlanScreen';

export const metadata = { title: 'Thực đơn tuần — MealMind' };

export default function MealPlanPage() {
  return <MealPlanScreen />;
}
