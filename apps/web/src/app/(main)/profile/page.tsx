import ProfileScreen from '@/features/profile/components/ProfileScreen';

export const metadata = { title: 'Hồ sơ — MealMind' };

export default function ProfilePage() {
  return <ProfileScreen />;
}
