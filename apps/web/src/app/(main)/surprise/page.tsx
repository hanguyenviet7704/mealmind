import SurpriseScreen from '@/features/home/components/SurpriseScreen';

export const metadata = { title: 'Surprise Me! — MealMind' };

export default function SurprisePage() {
  return <SurpriseScreen />;
}
