import SettingsScreen from '@/features/settings/components/SettingsScreen';

export const metadata = { title: 'Cài đặt — MealMind' };

export default function SettingsPage() {
  return <SettingsScreen />;
}
