import RandomRecipeScreen from '@/features/recipes/components/RandomRecipeScreen';

export const metadata = { title: 'Món ngẫu nhiên — MealMind' };

export default function RandomRecipePage() {
  return <RandomRecipeScreen />;
}
