import AddBookmarkScreen from '@/features/recipes/components/AddBookmarkScreen';

export const metadata = { title: 'Thêm món ăn — MealMind' };

export default function AddBookmarkPage() {
  return <AddBookmarkScreen />;
}
